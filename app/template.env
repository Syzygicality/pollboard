# DO NOT EDIT THIS FILE
# Create a .env file in this same directory and copy the following lines into it.
# replace the <placeholders> with your own information. Make sure that this user has full privileges over the pollboard database.
DB_ENGINE=django.db.backends.postgresql
DB_NAME=pollboard
DB_USER=<insert_db_user>
DB_PASSWORD=<insert_db_password>
DB_HOST=localhost
DB_PORT=5432

REDIS_SCHEME=redis
REDIS_HOST=localhost
REDIS_PASSWORD=<insert_redis_server_password>
REDIS_PORT=6379
REDIS_DB=1

OPENAI_KEY=
# CREATE USER <insert_db_user> WITH PASSWORD <insert_db_password>;
# CREATE DATABASE pollboard OWNER <insert_db_user>;
# python3 manage.py makemigrations
# python3 manage.py migrate